cat codecov.yml | curl --data-binary @- https://codecov.io/validate
comment: off

coverage:
  status:
    project:
      default:        
                target: auto
                threshold: 1%
      patch:
              default:
                      target: auto
                      threshold: 1%
